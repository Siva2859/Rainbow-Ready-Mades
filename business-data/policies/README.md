# Store Policies Intake Instructions

Document the verified, official operating rules for Rainbow Ready Mades here.
Use `policies_template.md` as the template structure. Replace all placeholders with exact rules provided by store management.
Do not invent policies; if a policy does not exist (e.g., store does not allow returns, only exchanges), accurately document that limitation.
